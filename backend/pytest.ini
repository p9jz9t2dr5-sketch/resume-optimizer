[pytest]
# Only collect the real suite. The repo root also holds throwaway dev scripts
# (e.g. test_polish.py) that need a running server and a live database, so they
# must not be picked up by pytest or CI.
testpaths = tests
